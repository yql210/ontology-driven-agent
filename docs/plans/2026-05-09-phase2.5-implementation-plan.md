# Phase 2.5 实施计划

> 日期: 2026-05-09
> 方案: docs/plans/2026-05-09-phase2.5-data-quality-remediation-design.md
> 审核分数: 7.5/10（可执行）

## Day 1: IMPORTS 关系修复

### Task 1.1: 修改 `_extract_import()` 提取逻辑
- **文件**: `src/layerkg/parser/python_parser.py`
- **位置**: `_extract_import()` 方法 (L297-339)
- **改动**: 将 `top_module = import_name.split(".")[0]` 改为 `last_segment = import_name.split(".")[-1]`
- **验证**: 现有测试通过 + 新增测试

```python
# 改前 (L329)
top_module = import_name.split(".")[0]

# 改后
last_segment = import_name.split(".")[-1]
# 并更新下面使用 top_module 的地方改为 last_segment
```

### Task 1.2: 修改 `_extract_import_from()` 提取逻辑
- **文件**: `src/layerkg/parser/python_parser.py`
- **位置**: `_extract_import_from()` 方法 (L341-377)
- **改动**: 
  1. 模块名取 `[-1]`
  2. 额外提取 `from X import A, B` 中的具名导入

```python
# 改前 (L365-367)
import_module = module_node.text.decode()
top_module = import_module.split(".")[0]

# 改后
import_module = module_node.text.decode()
last_segment = import_module.split(".")[-1]

# 新增：遍历 children 提取具名导入（非 module_node 的 identifier/dotted_name/aliased_import）
```

### Task 1.3: 修改 `_build_name_map()` 为多值映射
- **文件**: `src/layerkg/extractor/relation.py`
- **位置**: `_build_name_map()` (L57-67)
- **改动**: 返回 `dict[str, list[str]]` 而非 `dict[str, str]`

```python
# 改前
return {e.name: e.id for e in entities}

# 改后
mapping: dict[str, list[str]] = {}
for e in entities:
    mapping.setdefault(e.name, []).append(e.id)
return mapping
```

### Task 1.4: 重构 `resolve()` + 新增 `_resolve_relation()` + `resolve_with_unresolved()`
- **文件**: `src/layerkg/extractor/relation.py`
- **位置**: `resolve()` 方法 (L29-55)
- **改动**: 
  1. 新增 `_resolve_relation()` — 同文件优先匹配
  2. `resolve()` 内部改用多值映射
  3. 新增 `resolve_with_unresolved()`
- **验证**: 现有测试通过（返回值类型不变）

### Task 1.5: 修改 `_stage_parse()` 使用 `resolve_with_unresolved()`
- **文件**: `src/layerkg/builder.py`
- **位置**: `_stage_parse()` (L223)
- **改动**: 
  1. `relations = self._extractor.resolve(all_entities)` → `relations, unresolved_imports = self._extractor.resolve_with_unresolved(all_entities)`
  2. 返回值增加 `unresolved_imports`

```python
# 改前 (L223)
relations = self._extractor.resolve(all_entities)
return all_entities, doc_entities, relations, len(py_files) + len(doc_files)

# 改后
relations, unresolved_imports = self._extractor.resolve_with_unresolved(all_entities)
self._logger.info("Resolved %d relations, %d unresolved imports", len(relations), len(unresolved_imports))
return all_entities, doc_entities, relations, len(py_files) + len(doc_files), unresolved_imports
```

### Task 1.6: 修改 `_stage_write_structural()` 处理外部 import
- **文件**: `src/layerkg/builder.py`
- **位置**: `_stage_write_structural()` (L228-275)
- **改动**: 
  1. 签名增加 `unresolved_imports` 参数
  2. 为外部模块创建虚拟节点 + 写入关系
  3. 返回值改为三元组 `(graph_store, external_entity_count, external_rel_count)`

```python
def _stage_write_structural(
    self,
    all_entities: list[CodeEntity],
    doc_entities: list[DocEntity],
    relations: list[Relation],
    unresolved_imports: list[ExtractedRelation],  # 新增
) -> tuple[Neo4jGraphStore, int, int]:
    # ... 原有逻辑不变 ...
    
    # 新增：处理外部 import
    name_to_id = {e.name: e.id for e in all_entities}
    external_modules: dict[str, str] = {}  # name → id
    
    for ext_name in sorted({rel.target_name for rel in unresolved_imports}):
        ext_entity = CodeEntity(name=ext_name, entity_type="module", file_path="__external__", language="python")
        graph_store.merge_node("CodeEntity", self._entity_to_dict(ext_entity))
        external_modules[ext_name] = ext_entity.id
    
    ext_rel_count = 0
    for rel in unresolved_imports:
        source_id = name_to_id.get(rel.source_name)
        target_id = external_modules.get(rel.target_name)
        if source_id and target_id:
            graph_store.merge_relation(source_id, target_id, "imports", source_label="CodeEntity", target_label="CodeEntity")
            ext_rel_count += 1
    
    return graph_store, len(external_modules), ext_rel_count
```

### Task 1.7: 修改 `build()` 适配新的四元组→五元组 + 三元组返回
- **文件**: `src/layerkg/builder.py`
- **位置**: `build()` 中调用 `_stage_parse()` 和 `_stage_write_structural()` 的地方
- **改动**:
  1. `_stage_parse()` 返回五元组
  2. `_stage_write_structural()` 返回三元组
  3. `BuildResult.entities_created` 累加外部虚拟节点
  4. `BuildResult.relations_created` 累加外部 import 关系

```python
# Stage 1
all_entities, doc_entities, relations, files_scanned, unresolved_imports = self._stage_parse(repo_path)

# Stage 2
graph_store, ext_entity_count, ext_rel_count = self._stage_write_structural(
    all_entities, doc_entities, relations, unresolved_imports
)

# BuildResult
entities_created=len(all_entities) + len(doc_entities) + ext_entity_count,
relations_created=len(relations) + len(describes_rels) + ext_rel_count,
```

### Task 1.8: 新增 IMPORTS 相关测试
- **文件**: `tests/unit/test_python_parser.py` + `tests/unit/test_relation_extractor.py`
- **测试用例**:
  1. `test_import_from_extracts_last_segment` — 验证 `from layerkg.schema import X` → target="schema"
  2. `test_import_extracts_last_segment` — 验证 `import os.path` → target="path"
  3. `test_import_from_with_specific_names` — 验证 `from X import A, B` 额外提取 A, B
  4. `test_resolve_with_unresolved_external` — 验证外部 import 被归入 unresolved
  5. `test_resolve_same_name_file_priority` — 验证同名实体同文件优先

### Task 1.9: 验证
```bash
uv run pytest tests/ -q --tb=short
uv run ruff check src/ tests/
```

---

## Day 2: CALLS 关系提取

### Task 2.1: 新增 `_BUILTIN_NAMES` 常量
- **文件**: `src/layerkg/parser/python_parser.py`
- **位置**: 文件顶部，`PY_LANG` 之后
- **改动**: 新增 `frozenset` 定义（约 40 个内置函数/异常名）

### Task 2.2: 新增 `_extract_calls()` 方法
- **文件**: `src/layerkg/parser/python_parser.py`
- **位置**: `_extract_class()` 之后
- **改动**: BFS 遍历函数体，提取 call 节点，过滤 built-in + 短名

### Task 2.3: 在 `_extract_function()` 末尾调用 `_extract_calls()`
- **文件**: `src/layerkg/parser/python_parser.py`
- **位置**: `_extract_function()` 方法末尾 (L216 之后)
- **改动**: 添加一行调用

```python
# 在 relations.append(relation) 之后
self._extract_calls(node, source, file_path, relations, module_name, full_name)
```

### Task 2.4: 验证 CALLS resolve 策略
- **说明**: resolve() 已在 Day 1 改为多值映射+策略匹配，CALLS 关系自动走同文件优先
- **验证**: 新增测试确认

### Task 2.5: 新增 CALLS 相关测试
- **文件**: `tests/unit/test_python_parser.py`
- **测试用例**:
  1. `test_extract_simple_call` — `foo()` → calls foo
  2. `test_extract_method_call_self` — `self.method()` → calls method
  3. `test_extract_method_call_class` — `Class.method()` → calls method
  4. `test_extract_call_filters_builtin` — `print()` → 不生成关系
  5. `test_extract_call_filters_short_name` — `fn()` → 不生成关系
  6. `test_extract_nested_call` — `if foo(): bar()` → 提取两个 call
  7. `test_resolve_calls_same_file_priority` — 同名函数优先匹配同文件

### Task 2.6: 验证
```bash
uv run pytest tests/ -q --tb=short
uv run ruff check src/ tests/
```

---

## Day 3: SEMANTIC_IMPACT 提升

### Task 3.1: 修改 `_build_prompt()` 平衡两种关系权重
- **文件**: `src/layerkg/extractor/semantic.py`
- **位置**: `_build_prompt()` (L292-363)
- **改动**:
  1. 去掉 `## Step 2: ... (MOST IMPORTANT)` → 改为 `## Step 2: Identify Concepts`
  2. Step 1 增加 semantic_impact 示例模式
  3. Rules 中增加 "MUST identify both types"

具体修改：
```
# L318 改前
## Step 2: Identify Concepts (MOST IMPORTANT)

# L318 改后
## Step 2: Identify Concepts
```

新增 semantic_impact 引导（在 Step 1 下）：
```
Common semantic impact patterns:
- Data flow: parse() → validate() → transform() → store()
- Config dependency: functions that read the same config object
- Error propagation: function A catches errors from function B
- Side effects: function A modifies state that function B reads
```

修改 Rules：
```
# L358-362 之间新增
- You MUST identify both semantic_impact AND derived_from relations
- Aim for roughly equal numbers of each relation type
```

### Task 3.2: 验证 prompt 修改
- 小规模测试：`uv run layerkg build src/layerkg/parser --verbose-build`
- 检查 LLM 输出中 semantic_impact 数量是否增加

### Task 3.3: 验证
```bash
uv run pytest tests/ -q --tb=short
uv run ruff check src/ tests/
```

---

## Day 4: ModuleEntity 属性 + DocEntity 去孤立

### Task 4.1: 修改 `save_modules()` 补全属性
- **文件**: `src/layerkg/module_clustering.py`
- **位置**: `save_modules()` 方法
- **改动**:
  1. 签名增加 `all_entities` 参数
  2. 通过 entity_ids 查找实体，生成 description/size
  3. merge_node 增加两个字段

### Task 4.2: 修改 `builder.py` 调用 `save_modules()` 传入 `all_entities`
- **文件**: `src/layerkg/builder.py`
- **位置**: `_detect_and_write_modules()` 中调用 `save_modules()` 的地方
- **改动**: `clustering.save_modules(clusters)` → `clustering.save_modules(clusters, all_entities)`

### Task 4.3: 增强 `_link_docs_to_code()` 匹配策略
- **文件**: `src/layerkg/builder.py`
- **位置**: `_link_docs_to_code()` (L624-684)
- **改动**: 在现有路径匹配和代码块匹配之间，增加文件名匹配策略

新增匹配逻辑：
```python
# 策略2：文件名匹配（doc.content 包含 "module_name.py"）
# 在路径匹配之后、代码块匹配之前
if file_path:
    # 提取文件名（如 "schema.py"）
    file_name = Path(file_path).name
    if file_name in content:
        describes_rels.append(Relation(source_id=doc.id, target_id=entity_ids[0], relation_type="describes"))
        count += 1
        continue
```

### Task 4.4: 验证
```bash
uv run pytest tests/ -q --tb=short
uv run ruff check src/ tests/
```

---

## Day 5: 验证 ConceptEntity + 全量构建

### Task 5.1: 验证 ConceptEntity.entity_type 字段
- 运行 Cypher 查询确认数据正确
```cypher
MATCH (n:ConceptEntity) RETURN n.entity_type AS et, count(n)
```

### Task 5.2: 全量构建
```bash
uv run layerkg build src/layerkg --verbose-build
```

### Task 5.3: 数据质量复查
- 使用 `scripts/build-monitor.py` 或手写 Cypher 检查：
  - CALLS > 0 ✅
  - IMPORTS > 0 ✅（含项目内+外部）
  - SEMANTIC_IMPACT > 4 ✅
  - 孤立 DocEntity 占比下降 ✅
  - ModuleEntity 有 description/size ✅

### Task 5.4: 提交 + 推送
```bash
git add -A && git commit -m "feat(phase2.5): data quality remediation — CALLS, IMPORTS, SEMANTIC_IMPACT, ModuleEntity, DocEntity"
git push
```

---

## 工作量估计

| Day | Tasks | 预估时间 |
|-----|-------|----------|
| Day 1 | IMPORTS 修复 (9 tasks) | 30 min |
| Day 2 | CALLS 提取 (6 tasks) | 25 min |
| Day 3 | SEMANTIC_IMPACT (3 tasks) | 10 min |
| Day 4 | ModuleEntity + DocEntity (4 tasks) | 20 min |
| Day 5 | 全量构建验证 (4 tasks) | 150 min (含构建) |
| **总计** | **26 tasks** | **~235 min** |
